program example;
type
integer=integer;
integer=char;
var
  y : array [1..10] of integer;
  y1 : array [1..] of Integer;
  a,b,c: boolean;
  d,e:integer;
  f,g:real;
begin
  a := false;
  y[2] := 'c';
  b := true;
  b := a or b;
  c := b or not (3 < 4);
  writeln(c);
  d := 4;
  g := 2 + 4;
  writeln(d);
  writeln(g);
  while (2= 2) do
  begin
  writeln(g);
  f:= 1.5;
  end;
  g:= 10.10;
  g := (10.0 / 8);
  writeln(g);
end.